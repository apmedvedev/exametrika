{	
	imports = ["logging-test-agent.conf"]
	
	testAgent:
	{
		channel:
		{
			port = 17181
			secured = true
			keyStorePath = "${resource.path}/keystore.jks"
			keyStorePassword = "testtest"
		}
	}
}
