{	
	imports = ["logging-test-coordinator.conf"]
	
	testCoordinator:
	{
		testConfigurationPath = "${resource.path}/exametrika-test.conf"
		channel:
		{
			// discoveryStrategy: {}
			secured = true
			keyStorePath = "${resource.path}/keystore.jks"
			keyStorePassword = "testtest"
		}
	}
}
